export interface ExternalLodgingsAPIParametersModel {
    location: string,
    checkin: string,
    checkout: string,
    adults: number,
    children: number,
    infants: number,
    page: number,
    currency: string
}