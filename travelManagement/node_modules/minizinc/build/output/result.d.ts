import { IModelMetaData, IModelParams, IResult } from '../interfaces';
export declare function findOutputSeparator(output: string, start: number, findLast?: boolean): {
    index: number;
    sep: string;
    isHeader: boolean;
};
export declare class ResultBuilder {
    private readonly meta;
    private readonly params;
    private readonly callback?;
    private header;
    private extraHeader;
    private output;
    private solutions;
    private status;
    private statistics;
    constructor(meta: IModelMetaData, params: IModelParams, callback?: ((type: "solution" | "header", result: IResult) => void) | undefined);
    push(output: string): void;
    build(): IResult;
    private buildResult;
    private parseStats;
    private parsePartial;
}
export declare function parseResult(output: string, meta: IModelMetaData, params: IModelParams): IResult;
//# sourceMappingURL=result.d.ts.map