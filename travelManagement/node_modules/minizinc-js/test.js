const minizinc = require('./exc');

const model = `

`;

console.time('load');
minizinc
  .run(['--version']).then((out) => {
    console.timeEnd('load');
    console.log('done', out.stdout, out);
  });
